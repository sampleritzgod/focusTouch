# PostgreSQL connection (required for Prisma / API data)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/focustouch?schema=public"

# API
PORT=3001
NODE_ENV=development
CORS_ORIGIN="*"

# Clerk (Phase 1 — leave blank until auth is wired)
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
